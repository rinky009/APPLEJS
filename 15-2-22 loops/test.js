/*const order=[{userid:0,
               amount:10
              },
           {
                userid:2,
                amount:15
            } 
        
    ];
        const a=order.filter(order=>order.userid==2)
        .map(order=>order.amount);
        console.log(a);
        const arr=[{price:'100',product:'mobile'},
                   {price:'200',product:'laptop'}
];
arr.map((value)=>{
    console.log(value)
});
const arr=[1,2,3,4,5,6,7];
const newArr=arr.filter(a=>a%3==0);
console.log(newArr); */
